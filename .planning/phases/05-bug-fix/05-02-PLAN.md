---
phase: 05-bug-fix
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - index.html
  - app.js
autonomous: true
requirements:
  - BUG-01
  - BUG-04

must_haves:
  truths:
    - "回顾模式中关闭详情弹窗（X按钮/Escape/点击外部）停留在当前题目，不自动跳转"
    - "回顾模式中按左箭头回到上一道已答题，按右箭头前进到下一道已答题"
    - "回顾模式中按右箭头到最新题之后再按，退出回顾模式加载当前未答题"
    - "番剧导入不再出现完全无关的歌曲，评分阈值从20提升到50"
    - "低置信度匹配歌曲导入后弹窗让用户确认保留或删除"
  artifacts:
    - path: "index.html"
      provides: "回顾模式下右箭头按钮(nextQuestionBtn)DOM元素"
      contains: "nextQuestionBtn"
    - path: "app.js"
      provides: "closeDetailModal函数 + nextQuestion修改 + scoreImportResult阈值提升 + crossValidate函数 + 导入后低分确认弹窗"
      contains: "function closeDetailModal"
      contains: ">= 50"
      contains: "crossValidate"
      contains: "_importScore"
  key_links:
    - from: "app.js closeDetail action / Escape key / backdrop click"
      to: "closeDetailModal()"
      via: "函数调用替换nextQuestion()"
      pattern: "closeDetailModal\\(\\)"
    - from: "app.js ArrowRight / nextQuestion action"
      to: "nextQuestion()"
      via: "修改后的逐步前进逻辑"
      pattern: "viewingHistory.*questionIndex\\+\\+"
    - from: "app.js scoreImportResult"
      to: "searchItunesForAnime过滤逻辑"
      via: "阈值>=50 + crossValidate二次验证"
      pattern: ">= 50"
    - from: "app.js importFromBangumi"
      to: "低分确认弹窗"
      via: "lowConfSongs数组 + confirm对话框"
      pattern: "lowConfSongs"
---

<objective>
修复回顾模式导航逻辑（关闭弹窗留在当前题、右箭头逐步前进、右箭头可退出回顾模式），以及番剧导入匹配错误（提高评分阈值、添加交叉验证、导入后低分确认）。

Purpose: Bug 1当前所有关闭详情的操作（X/Escape/点击外部）都直接调用nextQuestion()跳回最新未答题，破坏回顾体验。Bug 4当前评分阈值20太低，缺少交叉验证，导致无关歌曲被导入。两个修复均不改动功能范围。
Output: index.html (新增nextQuestionBtn), app.js (closeDetailModal函数、nextQuestion修改、scoreImportResult阈值+交叉验证、导入后确认弹窗)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-bug-fix/05-CONTEXT.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md

<read_first>
<!-- These files MUST be read before any editing. Executor reads them in order to understand current state. -->
1. app.js:2492-2521 — 查看当前nextQuestion()和prevQuestion()函数完整实现
2. app.js:2517-2521 — 查看当前updatePrevButton()函数
3. app.js:2734 — 查看Escape键处理当前调用nextQuestion()的位置
4. app.js:2771 — 查看backdrop点击处理当前调用nextQuestion()的位置
5. app.js:2817 — 查看closeDetail action在事件委托中的映射
6. index.html:214-218 — 查看当前q-nav结构（仅有prevQuestionBtn）
7. app.js:1180-1250 — 查看scoreImportResult()和searchItunesForAnime()完整实现
8. app.js:1094-1173 — 查看importFromBangumi()完整实现
9. app.js:2800-2855 — 查看事件委托中所有data-action处理
</read_first>

<interfaces>
<!-- Key types and contracts the executor needs, extracted from current codebase. -->

From app.js nextQuestion() current implementation (line 2492-2504):
```js
function nextQuestion() {
    stopFullPlayer();
    $('animeDetailModal').classList.remove('show');
    if (gameState.viewingHistory) {
        // Return to the current unanswered question
        gameState.questionIndex = gameState.answerHistory.length;
        gameState.viewingHistory = false;
        loadQuestion();
    } else {
        gameState.questionIndex++;
        loadQuestion();
    }
}
```

From app.js prevQuestion() current implementation (line 2506-2515):
```js
function prevQuestion() {
    if (gameState.answerHistory.length === 0) return;
    if (!gameState.viewingHistory && gameState.questionIndex === 0) return;
    gameState.viewingHistory = true;
    stopFullPlayer();
    $('animeDetailModal').classList.remove('show');
    gameState.questionIndex--;
    if (gameState.questionIndex < 0) gameState.questionIndex = 0;
    loadQuestion();
}
```

From app.js updatePrevButton() (line 2517-2521):
```js
function updatePrevButton() {
    const btn = $('prevQuestionBtn');
    if (!btn) return;
    btn.style.display = gameState.questionIndex > 0 ? '' : 'none';
}
```

From index.html q-nav div (line 215-218):
```html
<div class="q-nav">
    <button class="q-nav-btn" id="prevQuestionBtn" data-action="prevQuestion" style="display:none;" aria-label="上一题">←</button>
    <div class="q-num"><span id="qNum">1</span><span>/</span><span id="totalQ">10</span></div>
</div>
```

From app.js Escape handler (line 2731-2736):
```js
if (e.key === 'Escape') {
    if (settingsOpen) { closeSettings(); return; }
    if (detailOpen) { nextQuestion(); return; }
    ...
}
```

From app.js backdrop click handler (line 2770-2774):
```js
if (e.target.id === 'animeDetailModal') { nextQuestion(); return; }
```

From app.js data-action switch (line 2804-2854):
```js
case 'closeDetail': nextQuestion(); break;
case 'nextQuestion': nextQuestion(); break;
case 'prevQuestion': prevQuestion(); break;
```

From app.js scoreImportResult threshold (line 1231):
```js
if (s >= 20) scored.push({ r, score: s });
```

From app.js importFromBangumi adding songs (line 1160-1162):
```js
const songs = await searchItunesForAnime(searchTitle, animeName, anime.name, year);
for (const song of songs) {
    if (addCustomSong(song)) addedCount++;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>任务 1：回顾模式导航逻辑修复 (D-01, D-02, D-03, D-04)</name>
  <files>index.html, app.js</files>
  <read_first>
1. app.js:2492-2521 — 查看当前nextQuestion()、prevQuestion()、updatePrevButton()的完整实现
2. app.js:2731-2736 — 查看Escape键处理中调用nextQuestion()的位置
3. app.js:2770-2774 — 查看backdrop点击处理中调用nextQuestion()的位置
4. app.js:2800-2855 — 查看事件委托switch中closeDetail、nextQuestion、prevQuestion的映射
5. app.js:2033-2085 — 查看loadQuestion()中回顾模式的逻辑和updatePrevButton()调用位置
6. index.html:214-218 — 查看当前q-nav DOM结构
  </read_first>
  <action>
执行以下精确修改，实现回顾模式导航修复：

**修改1 — app.js：新建closeDetailModal()函数**
在nextQuestion()函数之前插入：
```js
function closeDetailModal() {
    stopFullPlayer();
    $('animeDetailModal').classList.remove('show');
}
```
此函数仅关闭弹窗，不触发任何导航。关闭弹窗后用户停留在当前回顾的题目上（D-01）。

**修改2 — app.js：修改nextQuestion()的回顾模式逻辑**
修改nextQuestion()中gameState.viewingHistory为true时的代码块：
当前代码：直接跳到answerHistory.length并退出回顾模式。
改为：questionIndex自增1（逐步前进），仅当questionIndex >= answerHistory.length时才退出回顾模式：
```js
if (gameState.viewingHistory) {
    gameState.questionIndex++;
    if (gameState.questionIndex >= gameState.answerHistory.length) {
        gameState.viewingHistory = false;
        gameState.questionIndex = gameState.answerHistory.length;
    }
    loadQuestion();
}
```
非回顾模式下的else分支（questionIndex++后loadQuestion）保持不变。

**修改3 — app.js：Escape键处理改用closeDetailModal**
在keydown事件处理中（约line 2734），将：
`if (detailOpen) { nextQuestion(); return; }`
改为：
`if (detailOpen) { closeDetailModal(); return; }`

**修改4 — app.js：backdrop点击处理改用closeDetailModal**
在click事件处理中（约line 2771），将：
`if (e.target.id === 'animeDetailModal') { nextQuestion(); return; }`
改为：
`if (e.target.id === 'animeDetailModal') { closeDetailModal(); return; }`

**修改5 — app.js：事件委托closeDetail action改用closeDetailModal**
在data-action switch中（约line 2817），将：
`case 'closeDetail': nextQuestion(); break;`
改为：
`case 'closeDetail': closeDetailModal(); break;`

**修改6 — index.html：在q-nav中添加nextQuestionBtn**
在prevQuestionBtn之后、q-num div之前插入右箭头按钮：
```html
<button class="q-nav-btn" id="nextQuestionBtn" data-action="nextQuestion" style="display:none;" aria-label="下一题">→</button>
```
按钮初始隐藏，仅在回顾模式下显示。

**修改7 — app.js：更新updatePrevButton为updateNavButtons**
将updatePrevButton()函数重命名为updateNavButtons()并扩展逻辑：
```js
function updateNavButtons() {
    const prevBtn = $('prevQuestionBtn');
    const nextBtn = $('nextQuestionBtn');
    if (prevBtn) prevBtn.style.display = gameState.questionIndex > 0 ? '' : 'none';
    if (nextBtn) nextBtn.style.display = gameState.viewingHistory ? '' : 'none';
}
```
然后在app.js中所有调用updatePrevButton()的地方（当前仅有line 2083和line 2102）改为调用updateNavButtons()。

**修改8 — app.js：prevQuestion()中展示next按钮**
prevQuestion()函数在设置gameState.viewingHistory = true之后，已经调用了loadQuestion()。loadQuestion()中会调用updateNavButtons()，修改7已确保next按钮在viewingHistory为true时显示。无需额外修改prevQuestion。

**ArrowRight键盘快捷键验证：** 当前ArrowRight键处理（约line 2748）已调用nextQuestion()，修改2后nextQuestion在回顾模式下会questionIndex++逐步前进，到末尾自动退出回顾模式。ArrowRight快捷键无需额外修改。
  </action>
  <acceptance_criteria>
1. app.js中存在closeDetailModal函数 — grep验证: `grep -c "function closeDetailModal" app.js` 输出 >= 1
2. nextQuestion()中viewingHistory分支使用questionIndex++而非answerHistory.length赋值 — grep验证: `grep -c "questionIndex++" app.js` 输出 >= 2 (prevQuestion已有一次--，nextQuestion新增一次++)
3. Escape键处理调用closeDetailModal()而非nextQuestion() — grep验证: `grep -A2 "键 === 'Escape'" app.js | grep -c "closeDetailModal"` 输出 >= 1  （注意：中文键名）
4. backdrop点击处理调用closeDetailModal()而非nextQuestion() — grep验证: `grep -A1 "animeDetailModal.*click" app.js | grep -c "closeDetailModal"` 输出 >= 1
5. closeDetail action映射到closeDetailModal() — grep验证: `grep -A1 "closeDetail" app.js | grep -c "closeDetailModal"` 输出 >= 1
6. index.html中存在nextQuestionBtn — grep验证: `grep -c "nextQuestionBtn" index.html` 输出 >= 1
7. updateNavButtons函数存在 — grep验证: `grep -c "function updateNavButtons" app.js` 输出 >= 1
  </acceptance_criteria>
  <verify>
    <automated>grep -c "function closeDetailModal\|questionIndex++\|closeDetailModal()\|nextQuestionBtn\|function updateNavButtons" app.js index.html</automated>
  </verify>
  <done>回顾模式中关闭详情弹窗停留在当前题，右箭头逐步前进历史题目，右箭头到最新题之后再按退出回顾模式加载未答题。左箭头保持原有功能。nextQuestionBtn在回顾模式下可见。</done>
</task>

<task type="auto">
  <name>任务 2：番剧导入匹配错误修复 (D-10, D-11, D-12)</name>
  <files>app.js</files>
  <read_first>
1. app.js:1180-1250 — 查看scoreImportResult()函数的完整评分逻辑
2. app.js:1176-1250 — 查看searchItunesForAnime()完整实现（包含搜索循环和评分过滤）
3. app.js:1094-1173 — 查看importFromBangumi()完整实现（包含导入循环和状态更新）
4. app.js:118-200 — 查看addCustomSong()、getCustomSongs()、setCustomSongs()等自定义歌曲CRUD函数
  </read_first>
  <action>
执行以下精确修改，实现三重防护的导入匹配修复：

**修改1 — app.js scoreImportResult()：评分阈值从>=20提升到>=50 (D-10)**
在searchItunesForAnime()函数中（约line 1231），将：
`if (s >= 20) scored.push({ r, score: s });`
改为：
`if (s >= 50) scored.push({ r, score: s });`

**修改2 — app.js：在scoreImportResult()之后新增crossValidateAnime()交叉验证函数 (D-11)**
在scoreImportResult()函数定义之后、searchItunesForAnime()搜索循环之前插入crossValidateAnime函数：
```js
function crossValidateAnime(r, animeName, jpName) {
    const t = (r.trackName || '').toLowerCase();
    const c = (r.collectionName || '').toLowerCase();
    const targets = [
        (animeName || '').toLowerCase(),
        (jpName || '').toLowerCase()
    ].filter(Boolean);
    for (const target of targets) {
        if (t.includes(target) || c.includes(target)) return true;
        // Also check 3-char substrings for partial name matching (e.g. "咒術" from "咒術廻戦")
        for (let i = 0; i < target.length - 2; i++) {
            const sub = target.substring(i, i + 3);
            if (t.includes(sub) || c.includes(sub)) return true;
        }
    }
    return false;
}
```
然后在scored.push之前添加交叉验证检查。在`if (s >= 50)`条件中添加crossValidateAnime调用：
`if (s >= 50 && crossValidateAnime(r, animeName, jpName)) scored.push({ r, score: s });`

D-11满足：即使评分>=50，trackName或collectionName也必须包含番剧名的关键词片段才能通过。这防止了仅凭"已知歌手名"匹配的无关联曲被导入。

**修改3 — app.js searchItunesForAnime()：在歌曲对象上附加_importScore字段**
在searchItunesForAnime()中，生成results数组的每个song对象时（约line 1238-1245），添加_importScore属性：
在构建results.push({...})的对象中添加 `_importScore: s`字段（s来自scored数组中的score值）。修改results.push的调用，将result对象增加 `_importScore: s`。

具体来说，将当前的：
```js
for (const { r } of scored) {
    const title = r.trackName;
    if (seen.has(title)) continue;
    seen.add(title);
    results.push({
        title: title,
        titleCN: title,
        anime: animeName,
        artist: r.artistName || 'Unknown',
        year: year,
        type: guessSongType(title, r),
    });
}
```
改为：
```js
for (const { r, score } of scored) {
    const title = r.trackName;
    if (seen.has(title)) continue;
    seen.add(title);
    results.push({
        title: title,
        titleCN: title,
        anime: animeName,
        artist: r.artistName || 'Unknown',
        year: year,
        type: guessSongType(title, r),
        _importScore: score,
    });
}
```
注意：解构需要从`{ r }`改为`{ r, score }`。

**修改4 — app.js importFromBangumi()：导入后低分确认弹窗 (D-12)**
在importFromBangumi()函数中，添加低分歌曲收集和确认逻辑：

1. 在for循环之前声明 `const lowConfSongs = [];`
2. 在addCustomSong循环中（约line 1161-1163），添加低分跟踪：
```js
for (const song of songs) {
    if (addCustomSong(song)) {
        addedCount++;
        if (song._importScore && song._importScore < 80) {
            lowConfSongs.push(song);
        }
    }
}
```
3. 在导入循环完成后、最终状态更新之前（约line 1169之前），添加确认弹窗：
```js
if (lowConfSongs.length > 0) {
    await new Promise(r => setTimeout(r, 500)); // Let UI settle
    const names = lowConfSongs.map(s => `• ${s.title} (${s.anime})`).join('\n');
    const shouldRemove = confirm(
        `主人注意喵~ 以下 ${lowConfSongs.length} 首歌曲匹配度较低，可能和番剧无关：\n\n${names}\n\n是否移除这些歌曲？\n（点"取消"保留所有歌曲）`
    );
    if (shouldRemove) {
        const current = getCustomSongs();
        const toRemoveSet = new Set(lowConfSongs.map(s => `${s.title}|${s.anime}`));
        const filtered = current.filter(s => !toRemoveSet.has(`${s.title}|${s.anime}`));
        setCustomSongs(filtered);
        addedCount -= lowConfSongs.length;
        if (statusEl) statusEl.textContent = `导入完成！保留 ${addedCount} 首歌曲（已移除 ${lowConfSongs.length} 首低匹配合）`;
    }
}
```

D-12满足：导入完成后弹窗列出低分歌曲，用户可选择移除。
  </action>
  <acceptance_criteria>
1. scoreImportResult过滤阈值>=50 — grep验证: `grep -c "s >= 50" app.js` 输出 >= 1
2. crossValidateAnime函数存在 — grep验证: `grep -c "function crossValidateAnime" app.js` 输出 >= 1
3. searchItunesForAnime中调用crossValidateAnime — grep验证: `grep -c "crossValidateAnime" app.js` 输出 >= 2
4. 歌曲对象包含_importScore字段 — grep验证: `grep -c "_importScore" app.js` 输出 >= 1
5. importFromBangumi中存在lowConfSongs收集逻辑 — grep验证: `grep -c "lowConfSongs" app.js` 输出 >= 2
6. importFromBangumi中存在confirm确认弹窗 — grep验证: `grep -c "主人注意喵.*匹配度较低" app.js` 输出 >= 1
7. 原有的`>= 20`不再存在（已改为>=50）— grep验证: `grep -c "s >= 20" app.js` 输出 == 0
  </acceptance_criteria>
  <verify>
    <automated>grep -c "s >= 50\|function crossValidateAnime\|_importScore\|lowConfSongs\|主人注意喵" app.js</automated>
  </verify>
  <done>番剧导入评分阈值从20提升到50，trackName或collectionName必须包含番剧名关键词才能通过，低置信度(50-79分)歌曲导入后弹窗让用户确认保留或删除。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增 | 本计划仅修改现有前端代码，不引入新的trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Spoofing | crossValidateAnime关键词匹配 | accept | 关键词匹配是防止误导入的辅助验证，不涉及安全边界。即使被绕过，最多导入无关歌曲，不影响现有数据 |
| T-05-04 | Information Disclosure | confirm弹窗中的歌曲名称 | accept | 弹窗中显示的歌曲名称来自iTunes API返回数据，已在客户端上下文中，无额外泄露风险 |

本计划不涉及npm/pip/cargo包安装，无需Package Legitimacy Gate。
</threat_model>

<verification>
1. 本地启动 `python -m http.server 8080`
2. 测试回顾模式导航：
   - 答几道题 → 左箭头进入回顾 → 点击歌曲打开详情 → X/Escape/点击外部关闭详情 → 确认留在同一题
   - 按右箭头 → 前进到下一道回顾题 → 到最后一题后再按右箭头 → 退出回顾模式加载未答题
3. 测试导入匹配：
   - 导入一个番剧目录（如75323）→ 确认新导入的歌曲都和番剧相关
   - 如果有低分歌曲 → 确认弹窗出现并可以移除/保留
</verification>

<success_criteria>
1. 回顾模式中关闭详情弹窗停留在当前题目，右箭头可逐步浏览历史题 (BUG-01)
2. 番剧导入不再出现完全无关的歌曲，低置信度匹配可手动确认 (BUG-04)
</success_criteria>

<output>
创建 `.planning/phases/05-bug-fix/05-02-SUMMARY.md` 完成后
</output>
