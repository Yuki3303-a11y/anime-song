# 萌豚挑战 · 优化方案 v1

> 日期：2026-09-05
> 范围：基于用户确认的方向（玩法增强 + 曲库自动更新 + 技术健康），不含 PK 直连与账号体系
> 状态：待确认 → 确认后按 P0 → P1 顺序实施

---

## 一、目标与范围

### 本次要做

| 方向 | 内容 | 优先级 |
|------|------|--------|
| 玩法 | 猜歌名 / 猜歌手模式（不止猜番剧） | P0 |
| 玩法 | 提示系统（歌手 / 歌名提示，扣分制） | P0 |
| 曲库 | 自动更新落地（AnimeThemes + AniList + Actions + PR 复核） | P1 |
| 技术 | 文档同步、曲库校验脚本固化（低成本高价值项） | P1 |
| 技术 | B站代理加固（可选，待确认） | P1 |

### 明确不做（本次排除）

- PK 联机国内直连（依赖 Firebase，维持现状）
- 账号体系 / 云端同步（收藏、记录、排行榜保持 localStorage）
- 每日挑战、生存模式 / 限时赛
- 成绩分享卡片、成就系统

---

## 二、P0 · 玩法增强（预计 2-3 人日）

### 2.1 猜歌名 / 猜歌手模式

**现状**：只能猜番剧，`songs.js` 已有 `title` / `titleCN` / `artist` 字段，数据完全复用，无需扩库。

**设计**：

1. **入口**：主菜单「单人挑战」改为弹模式选择：猜番剧 / 猜歌名 / 猜歌手 / 混合模式。
   - 也可在菜单直接放 3 个按钮（改动更小，推荐先做按钮版）
2. **猜歌名**：选项显示歌名（`titleCN` 优先，空则用 `title`）。
   - 干扰项：从歌曲池随机抽 3 首，**排除与正确答案同番剧、同歌手**（同番 OP/ED 互作干扰项难度过高，作为后续"硬核模式"选项）
3. **猜歌手**：选项显示歌手名。
   - 干扰项：从歌手池（`ALL_ARTISTS` 新导出，由 songs 去重生成）随机抽 3 个，排除正确答案歌手
4. **计分 / 连击 / 排行榜**：完全复用现有逻辑；排行榜记录增加模式字段（如 `g: 'song' | 'artist'`），旧记录兼容显示
5. **键盘快捷键 1-4、筛选（类型/来源）**：共用不变
6. **PK 模式**：本次保持猜番剧不变（避免 Firebase 数据结构变更）

### 2.2 提示系统

**设计**：

| 模式 | 提示 1 | 提示 2 |
|------|--------|--------|
| 猜番剧 | 显示歌手名 | 显示歌名首字 + 字数 |
| 猜歌名 | 显示歌手名 | 显示番剧名 |
| 猜歌手 | 显示歌名 | 显示番剧名 |

- 每题提供 2 个提示按钮（点击后置灰、不可重复使用）
- **扣分制**：使用提示后本题得分按固定比例折算（如 ×0.6），不打断连击
- 已在答题详情卡展示的歌手/歌名信息不构成泄露（答题后才展示）
- 纯前端逻辑，无需新数据字段、无需改 API

**验收标准**：
- [ ] 用提示后本题得分正确折算并显示
- [ ] 提示按钮用过即置灰，不能撤销
- [ ] 键盘 1-4 答题不受提示影响
- [ ] 三个模式 + 混合模式提示内容均正确

---

## 三、P1 · 曲库自动更新落地（预计 3-4 天）

### 3.1 发现脚本 `scripts/fetch-season.mjs`

流程（对齐 `feasibility-auto-library.md` 已验证链路）：

```
① AniList: season + seasonYear, sort=POPULARITY_DESC, isAdult:false
   → 取前 40-50 部（含 popularity / averageScore）
② AnimeThemes: 每部取 OP1 / ED1（type / song.title / song.artists）
   ⚠️ 注意 filter 数组编码（filter[anime.season][]=summer）与 include 逗号分隔
③ 质量门禁：
   - popularity ≥ 阈值（首次试跑后校准，建议先取全季前 40-50 名）
   - 每番剧限 1-2 首（OP1 + ED1 优先）
   - 与现有 SONGS 去重（title|anime 归一化匹配，复用曲库重建脚本逻辑）
④ 输出 candidates.js：候选清单 + 每首的来源链接 / 热度数据 / 建议 anime 名
```

### 3.2 GitHub Actions 定时 + PR 复核

- 新增 `.github/workflows/season-crawl.yml`
- 每周一 09:00（UTC+8）触发；新番开播后第 2-4 周开始生效
- 有候选变更才创建 PR；无变更则静默结束
- 人工复核（10 分钟）：过目候选 → 合并 → bump 版本号 v25→v26（app.js import + index.html script）

### 3.3 titleCN 策略

- **阶段一（本次）**：`titleCN = title`（罗马音/原名），与现库约 70% 条目一致
- **阶段二（后续增强）**：Bangumi 反查番剧官方中文名；歌名翻译可选

### 3.4 首次试运行

- 用 **2026 夏季档**（已开播）试跑 → 校准热度阈值 → 产出第一批候选 PR
- 验证项：候选数量、榜单命中率、去重正确性、titleCN 可用性

### 风险与对策

| 风险 | 概率 | 对策 |
|------|------|------|
| AnimeThemes 新番录入延迟 | 高 | 开播后 2-4 周再跑；脚本加重试 + 指数退避 |
| 热度阈值导致候选过少/过多 | 中 | 首次试运行校准；或交叉验证 YouTube 播放量 |
| iTunes 无新歌试听 | 低 | 现有 YouTube→B站降级链自动兜底 |
| Actions 触发权限/限额 | 低 | 公开仓免费额度充足 |

---

## 四、P1 · 技术健康（低成本高价值）

### 4.1 文档同步（约 0.5 人日以内）

| 文件 | 需要修正 |
|------|----------|
| README.md | 曲库数量 562 → **285 首 / 188 部**；更新曲库说明段落 |
| ARCHITECTURE.md | 各处"562 首"字样 → 285 |
| CLAUDE.md | 同上（song 数据段） |
| overview.md | 已是最新，不动 |

### 4.2 曲库校验脚本固化（约 0.5 人日）

- 把 CLAUDE.md 里的临时 node 去重命令固化为 `scripts/validate-songs.mjs`：
  - 去重（title|anime 唯一）
  - 五字段完整性
  - 每番剧配额 ≤ 2（当前策略）
  - type 合法性（OP/ED/IN）
  - `ALL_ANIME` / `AVAILABLE_TYPES` 与实际集合一致
  - 版本号一致性检查（app.js / index.html / songs.js 缓存版本号）
- `package.json` 增加 `npm run validate`；可选接 GitHub Actions（push 时跑）

### 4.3 B站代理加固（可选，约 1 人日，**待你确认是否做**）

- `api/search.js`：WBI keys 获取失败时自动重试而非直接 fallback；上游超时（如 10s）与 5xx 错误码处理；流代理断流重连
- 现状问题：BILI_COOKIE 硬编码（可能过期）、无超时控制、无重试

---

## 五、实施顺序与依赖

```
P0-1 猜歌名/歌手模式（含模式选择入口）      ← 独立
  ↓（提示内容按模式适配，依赖模式存在）
P0-2 提示系统                               ← 依赖 P0-1 或并行实现

P1-1 fetch-season.mjs 脚本                  ← 独立
P1-2 GitHub Actions + PR 流程               ← 依赖 P1-1
P1-3 首次夏季档试运行 + 阈值校准            ← 依赖 P1-2
P1-4 文档同步 + 校验脚本                    ← 独立，可随时并行

交付：本地验证 → 版本号 bump → 推送（Vercel / GitHub Pages 自动部署）
```

**建议顺序**：先做 P0（玩家立即可感知）→ 再做 P1 曲库（周期长、见效慢）→ P1 技术健康穿插并行。

---

## 六、验证方式

- P0：`python -m http.server 8080` 本地起服，三个模式 + 混合模式各跑 1 局，核对提示扣分、干扰项去重、排行榜记录
- P1：`node scripts/fetch-season.mjs --season summer --year 2026` 真实试跑；`npm run validate` 通过
- 交付前：`node --check` 全部改动文件；曲库版本号一致性检查

---

## 七、待确认事项

1. **B站代理加固（4.3）**：做 / 不做 / 延后？
2. **模式入口**：主菜单放 3 个独立按钮，还是单人挑战弹模式选择窗？
3. **提示扣分比例**：×0.6 / ×0.5 / 固定扣 10 分？混用提示（两个都点）怎么算？
4. **猜歌名干扰项**：严格排除同番/同歌手（推荐），还是允许同番剧歌互作干扰项（硬核）？
5. **曲库目标规模**：保持 285 精选（自动更新只补新番），还是同时按榜单扩到 400+？
6. 方案确认后，按 P0 → P1 顺序实施。
